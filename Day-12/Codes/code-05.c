#include<stdio.h>
int main(){
	int firstNumber,secondNumber=25;
	/*Here only the second variable (secondNumber) will 
	get that value(25),first variable(firstNumber) will 
	show the random number*/
	printf("\nFirst number: %d",firstNumber);
	printf("\nSecond number:%d", secondNumber);
	return 0;
}